# Copy to node-<i>/validator.env (or run generate_keys.sh) — KEEP SECRET (gitignored).
NODE_INDEX=0
NODE_HOST=10.0.0.1
POS_VALIDATOR_KEY=<64 hex seed (ML-DSA) or 128 hex (dual)>
VALIDATOR_PUBKEY=<hex of prefixed pubkey>
PUBKEY_HASH=<hex of sha256hash160(pubkey)>
ADDRESS=<base58 address>
