# Bigtangle: スケーラブルなマルチレイヤーブロックチェーン

## Bigtangleの仕組み

Bigtangleは2つの技術を組み合わせたものです：

- **DAG（有向非巡回グラフ）** により、多数のトランザクションを
  同時に処理し、高速性を実現します。
- **Proof-of-Stake（PoS）ビーコンチェーン** が、定期的にそれらのトランザクションを
  確定し、永続化します。

DAGを交通を迅速に流す高速道路と考えてください。一方、ビーコンチェーンは
交通管理局のように定期的にすべてが正しいことを
認証します。

12秒ごとに、1人のバリデータが選ばれて**ビーコンブロック**を作成します。この
ビーコンは最近のすべてのトランザクション活動を確定し、それを不可逆にします。

---

## Bigtangleの理由

| 問題 | Bigtangleによる解決方法 |
|---------|------------------------|
| **遅いトランザクション** — イーサリアムL1は約30 tx/s、ビットコインは約7 tx/s | DAG並列処理：毎秒数千のトランザクションを並列処理 |
| **単一リーダーリスク** — 指定されたリーダーが失敗すると、チェーンが停止する | トランザクション処理に単一リーダーのボトルネックがない。DAGブロックは多数のノードによって並列に作成できる。ビーコンブロックはファイナリティを提供するためにスロットごとに1人のバリデータによって提案される。 |
| **遅いファイナリティ** — イーサリアムは不可逆的な確定に約6分かかる | Casper FFGによるファイナリティは約12.8分（2エポック）。ビーコンブロックは約24秒で確定 |
| **水平スケーリング不可** — ほとんどのチェーンは単一の台帳で動作する | 多数のL1アプリケーションチェーン。それぞれが独自のバリデータとコンセンサスを持つ |
| **量子脆弱性** — ECDSA署名は量子コンピュータで破られる可能性がある | 2つのNIST承認アルゴリズムによるデュアル耐量子署名 |

---

## アーキテクチャ

Bigtangleは2つの独立したレイヤーで動作します：

```
              ビーコンチェーン（セキュリティ）

ビーコン1 -------- ビーコン2 -------- ビーコン3
      |                |                 |
      | 確定する       | 確定する        | 確定する
      v                v                 v

         トランザクションDAG

       A
      / \
     B   C
      \ /
       D
      / \
     E   F
```

- **DAG**はトランザクションスループットを処理します。多くのブロックを
  同時に作成できるため、ネットワークはどの既存ブロックに
  新しいブロックが接続すべきかを決定する必要があります。Bigtangleは
  ステーク加重GHOSTルールを使用してこれを解決します。新しいブロックは
  累積ステーク加重が最も大きい子ブロックに接続され、最重ブランチが
  勝ちます。代替パスの成長も引き続き許容されます。

- **ビーコンチェーン**はファイナリティを提供します。12秒ごとに、バリデータが
  偏りのないランダムプロセスによって選ばれ、最近のDAG活動を確定する
  ビーコンブロックを生成します。Bigtangleはイーサリアムと同じ
  ファイナリティメカニズム（Casper FFG）を使用します。3分の2以上の
  バリデータが合意すると、確定されたブロックは永続的になります。

2つのレイヤーは補完的です：DAGは速度と並列処理を提供し、
ビーコンチェーンはセキュリティとファイナリティを提供します。

---

## DAGと並列処理

### DAGの仕組み

従来のブロックチェーンでは、ブロックは一列に次々と作成されます。これにより
ボトルネックが発生します — 全員が次のブロックを待たなければなりません。

BigtangleはDAGを使用し、各ブロックは1つではなく2つの前のブロックに接続
します。これにより、複数のブロックを同時に生成できます：

```
    ┌───┐     ┌───┐     ┌───┐
    │ G │────►│ A │────►│ B │────► ...
    └───┘     └───┘     └───┘
       \         \
        \         └───┐     ┌───┐
         └───┐    │ C │────►│ D │
              │    └───┘     └───┘
              │
              └───┐    ┌───┐
                  │ E │────► ...
                  └───┘
```

主な利点：

- **複数ブロックの並列処理** — 単一リーダーを待つ必要がない。
- **空きスロットなし** — バリデータが順番を逃しても、DAGが吸収する。
- **より高速な確認** — トランザクションは数分ではなく数秒で
  確認される。

### チップ選択の仕組み

新しいブロックが作成されるとき、接続先の既存ブロックを2つ選ぶ必要が
あります。ネットワークは最近のブロックに対してランダム化検索を実行し、
自然に最も健全なブランチを優先しながら、代替パスの成長も許容します。
これにより、高負荷下でも新しいブロックが常にDAGの最も接続の良い部分に
アタッチされることが保証されます。

### トランザクション検証

すべてのトランザクションはネットワークに入る前にチェックされます：

1. **フォーマットチェック** — 重複する入力、負の値がないこと。
2. **署名検証** — 送信者の署名は
   即座に検証されます。
3. **二重支払い防止** — 同じ資金をペンディングキュー内で
   2回使用することはできません。
4. **手数料チェック** — 各トランザクションは最低手数料を含まなければなりません。

無効なトランザクションは、ブロックが生成される時ではなく、送信時に
拒否されます。

---

## Proof of Stakeとファイナリティ

### バリデータの選出方法

12秒ごとに、1人のバリデータがランダムに選ばれてビーコンブロックを
生成します。選出には、いかなる参加者も予測したり操作したりできない、
偏りのないランダム性ソースが使用されます。

バリデータは最低3200万BIGを預けて登録します。バリデータは
不正行為（例：競合するブロックへの署名）を行った場合にペナルティが科せられます。
ペナルティにはステークされた資金の没収が含まれます。

### ファイナリティの仕組み

各エポックの終わり（32スロットごと、約6.4分）に、
ネットワークはどのブロックが十分なバリデータの支持を得たかを評価します：

- ブロックは、ステークされたバリデータの3分の2が
  それにアテステーションしたとき、**ジャスティファイド**になります。
- ブロックは、それが拡張するチェーンが既に
  ファイナライズされていたとき、**ファイナライズド**になります。
- 一度ファイナライズされると、ブロックは決して元に戻せません（
  ステークされた全バリデータの3分の1が共謀しない限り）。

### 手数料プールとバリデータ報酬

システムは**ゼロインフレーション**です — 新しいトークンはブロック補助金として
鋳造されません。BIGの総供給量はジェネシス時に固定され、増加することはありません。

すべてのトランザクションは手数料を支払います。手数料は各エポックを通じて蓄積され、
エポック境界でバリデータに分配されます。各バリデータは
ステークした量に比例した報酬を受け取ります。これによりインセンティブが一致します：
バリデータはより多くのリソースをネットワークにコミットすることでより多く獲得し、
報酬プール全体はネットワーク使用料から完全に発生するのであって、通貨の拡張に
よるものではありません。

---

## マルチレイヤーアーキテクチャ

### 1つのLayer 0、多数のLayer 1チェーン

世界には**正確に1つのLayer 0チェーン**があります。これはセツルメント
チェーンであり、ネイティブトークン（BIG）が作成され、カスタムトークンが
発行されます。Layer 0はトークン供給の真実の源であり、すべての価値移転のルートです。

**多数のLayer 1チェーン**が存在でき、それぞれが独自のバリデータ、データベース、
コンセンサスを持って独立して動作します：

```
                    Layer 0（セツルメント）
                           │
            ┌───────────────┼───────────────┐
            │               │               │
       L1-order        L1-contract       L1-payment
     （注文マッチング） （スマートコントラクト） （転送のみ）
            │               │               │
       L1-PAI           L1-NFT           （さらにL1...）
     （AIプロバイダー） （非代替性）
```

各L1チェーン：
- 独自のバリデータとコンセンサスを持つ。
- L0からのブリッジを介してのみBIGトークンを受け取る（ネイティブな鋳造はなし）。
- 完全に分離されている — あるL1の障害は他に影響しない。
- 独自の`chainId`を使用して、あるチェーンのブロックが別のチェーンに
  受け入れられるのを防ぐ。

### 水平スケーラビリティ

各L1チェーンは完全に分離されており、チェーンIDが設定可能であるため、
同じタイプの複数のインスタンスを実行できます：

```
CHAIN_ID=PAYMENT-US  →  USリージョン用支払いチェーン
CHAIN_ID=PAYMENT-EU  →  EUリージョン用支払いチェーン
```

各インスタンスはL0と同等のスループットを達成します。システム全体の
スループットはL1インスタンスの数に比例して線形にスケーリングし、クロス
チェーン調整のオーバーヘッドはありません。

### 利用可能なチェーンタイプ

| チェーン | 目的 |
|-------|---------|
| L0 セツルメント | BIGの鋳造、トークン作成、グローバルアンカー |
| L1 注文マッチング | 分散型オーダーブックマッチング |
| L1 スマートコントラクト | 汎用コントラクト実行 |
| L1 AIプロバイダー | AIプロバイダーのステーキングとレピュテーション |
| L1 NFT | 非代替性トークンの作成と転送 |
| L1 支払い | 転送のみ（最小限の攻撃対象領域） |

---

## 耐量子セキュリティ

Bigtangleはすべてのトランザクション入力に2つの耐量子署名アルゴリズム
を使用します。どちらもNIST承認済みの最終標準です：

| アルゴリズム | 標準 | セキュリティレベル |
|-----------|----------|---------------|
| ML-DSA-87（Dilithium） | FIPS 204 | カテゴリ5 |
| SLH-DSA-SHA2-256s（SPHINCS+） | FIPS 205 | カテゴリ5 |

2つのアルゴリズムは数学的に独立した仮定（格子暗号とハッシュ関数）に
依存しています。一方を破っても他方は破れません。
攻撃者は単一のトランザクションを偽造するために
両方を同時に破らなければなりません。

標準のBIP39シードフレーズが両方の鍵ペアを決定論的に生成します。
もしアルゴリズムがいつか破られた場合、システムはトランザクション形式を変更したり、
既存の資金を無効にしたりすることなく、代替アルゴリズムへの
アップグレードをサポートします。

---

## 比較

| 指標 | Bigtangle | Solana | Ethereum L1 | Visa |
|--------|-----------|--------|-------------|------|
| 台帳 | DAG + ビーコンチェーン | 単一チェーン | 単一チェーン | 中央集権 |
| コンセンサス | GHOST + Casper FFG | PoH + Tower BFT | Gasper | 権威 |
| スロット時間 | 12s | 400ms | 12s | — |
| ファイナリティ | 約12.8分* | 約12.8s | 約6.4分 | 即時 |
| ピーク tx/s | 約4,873 | 約50,000* | 約30 | 24,000 |
| 観測 tx/s | 約4,873 | 約2,000–3,000 | 約15–30 | 約1,700 |
| 並列実行 | DAGネイティブ | Sealevel（分析） | 逐次EVM | シャーディングDB |

\*ベンダー報告のラボピーク値。観測されたメインネットスループットは
大幅に低い。

\*\*Casper FFGファイナリティ（2エポック）。ビーコンブロック確認約24s（2スロット）。

### 主な利点

- **単一リーダーのボトルネックなし** — トランザクションブロックは
  どのノードでも作成可能。ビーコンブロック提案者はスループットを
  制限することなくファイナリティを提供する。
- **水平スケーリング** — 追加のL1チェーンがプロトコル変更なしで
  容量を増やす。
- **UTXOモデル** — トランザクションは異なる入力を参照するため、
  本質的に並列処理が可能。
- **耐量子セキュリティ** — デュアル署名がローンチ時から有効。

---

## 結論

Bigtangleは、高スループットのための並列DAGと、決定論的ファイナリティのための
Proof-of-Stakeビーコンチェーンを組み合わせています。このアーキテクチャは、
従来のブロックチェーンに見られる単一リーダーのボトルネックを排除しつつ、
強力なセキュリティ保証を維持します。

Layer 0はグローバルなセツルメントを提供します。独立したLayer 1チェーンは
クロスチェーン調整のオーバーヘッドなしに、特殊なアプリケーションにわたる
水平スケーリングを可能にします。

これらの設計選択は、統一されたアーキテクチャ内で、高いトランザクション
スループット、迅速な確認、モジュール式のスケーラビリティ、固定されたトークン
供給、そして耐量子セキュリティを実現します。

---

## 付録：開発者ガイド

### ノード設定

| モジュール | ポート | デフォルトDB | 役割 |
|--------|------|------------|------|
| `layer0-server` | 8081 | `info_l0` | L0フルノード |
| `l1-order-server` | 8083 | `info_order` | L1注文マッチング |
| `l1-payment-server` | 8091 | `info_payment` | L1支払い |
| `l1-pai-server` | 8087 | `info_pai` | L1 AIプロバイダー |
| `l1-nft-server` | 8089 | `info_nft` | L1 NFT |

### 環境変数

| 変数 | デフォルト | 説明 |
|----------|---------|-------------|
| `CHAIN_ID` | （可変） | L1チェーン識別子 |
| `FEE_DEFAULT` | 1000 | 最低トランザクション手数料（BIG） |
| `POS_SLOT_INTERVAL_MS` | 12000 | スロット間隔（ms） |
| `POS_SLOTS_PER_EPOCH` | 32 | エポックあたりのスロット数 |

### チェーンごとのブロックタイプスコープ

| チェーン | 受け入れられるブロックタイプ |
|-------|---------------------|
| L0 | すべてのタイプ |
| L1 注文マッチング | INITIAL, TRANSFER, BEACON, ORDER_OPEN, ORDER_CANCEL |
| L1 コントラクト | INITIAL, TRANSFER, BEACON, CONTRACT_EVENT, CONTRACTEVENT_CANCEL |
| L1 PAI | INITIAL, TRANSFER, BEACON, CONTRACT_EVENT, CONTRACTEVENT_CANCEL |
| L1 NFT | すべてのタイプ（L0パラメータを再利用） |
| L1 支払い | INITIAL, TRANSFER, BEACONのみ |

### 報酬計算

各エポック境界で、蓄積された手数料が分配されます：

```
アクティブなバリデータごとに：
  報酬 = バリデータ.ステーク × プール / 総アクティブステーク
```

### ブロックごとの手数料蓄積

```
ブロック内のトランザクションごとに：
  余剰 = BIG入力の合計 − BIG出力の合計
  もし余剰 > 0 なら：
    余剰を accumulated_fee_pool に追加
```

### 耐量子鍵導出

24単語のBIP39シードフレーズ（256ビットエントロピー）は、HKDF-SHA256を
介して両方の耐量子鍵ペアを決定論的に生成します。最初の32バイトが
ML-DSA-87をシードし、2番目の32バイトがSLH-DSA-SHA2-256sをシードします。

### ブロックサイズ予算（耐量子署名）

| コンポーネント | サイズ |
|-----------|------|
| ML-DSA-87署名 | 4.6 KB |
| SLH-DSA-256s署名 | 16 KB |
| 入力あたりの合計 | 約23 KB |
| 100 tx/ブロック | 約3 MB（実現可能） |
| 500 tx/ブロック | 約12 MB（実現可能） |
| 2,000 tx/ブロック | 約48 MB（100 MB制限が必要） |
