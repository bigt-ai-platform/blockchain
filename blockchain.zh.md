# Bigtangle: 可扩展的多层区块链

## Bigtangle 的工作原理

Bigtangle 结合了两项技术：

- **DAG（有向无环图）**，可同时处理大量交易，实现高速度。
- **权益证明（PoS）信标链**，定期确认这些交易并使其永久化。

可以将 DAG 视为一条快速移动交通的高速公路，而信标链则充当交通管理机构，定期认证一切是否正确。

每 12 秒，系统选择一个验证者创建一个**信标块**。该信标确认所有最近的交易活动并使其不可逆转。

---

## 为什么选择 Bigtangle

| 问题 | Bigtangle 的解决方案 |
|---------|------------------------|
| **交易速度慢** — 以太坊 Layer 1 约 30 tx/s，比特币约 7 tx/s | DAG 并行：每秒数千笔交易并行处理 |
| **单一领导者风险** — 如果指定的领导者失败，链就会停滞 | 交易处理没有单一领导者瓶颈，因为 DAG 块可由多个节点并行创建。信标块由每个时隙一个验证者提出以提供最终性。 |
| **最终性慢** — 以太坊需要约 6 分钟才能实现不可逆结算 | Casper FFG 最终性约 12.8 分钟（2 个纪元）；信标块约 24 秒确认 |
| **无水平扩展** — 大多数链在单一账本上运行 | 多个 L1 应用链，每个链有自己的验证者和共识 |
| **量子漏洞** — ECDSA 签名可被量子计算机攻破 | 使用两种 NIST 批准的算法进行双重后量子签名 |

---

## 架构

Bigtangle 运行两个独立的层：

```
              信标链（安全层）

信标 1 -------- 信标 2 -------- 信标 3
     |                |                 |
     | 确认           | 确认            | 确认
     v                v                 v

        交易 DAG

      A
     / \
    B   C
     \ /
      D
     / \
    E   F
```

- **DAG** 负责交易吞吐量。由于许多块可以同时创建，网络需要一种方法来确定新块应连接到哪些现有块。Bigtangle 通过对最近区块进行随机化搜索来解决此问题。该搜索自然倾向于连接良好的分支，同时仍然允许替代路径增长。（这种技术称为马尔可夫链蒙特卡洛，即 MCMC。）

- **信标链**提供最终性。每 12 秒，由一个无偏随机过程选出的验证者产生一个信标块，确认最近的 DAG 活动。Bigtangle 使用与以太坊相同的最终性机制（Casper FFG）。一旦三分之二的验证者达成一致，被确认的块就成为永久性的。

这两个层是互补的：DAG 提供速度和并行性；信标链提供安全性和最终性。

---

## DAG 与并行处理

### DAG 的工作原理

在传统区块链中，块按顺序一个接一个地创建。这造成了瓶颈——每个人都必须等待下一个块。

Bigtangle 使用 DAG，其中每个块连接到两个前驱块而不是一个。这允许多个块同时产生：

```
    ┌───┐     ┌───┐     ┌───┐
    │ G │────►│ A │────►│ B │────► ...
    └───┘     └───┘     └───┘
       \         \
        \         └───┐     ┌───┐
         └───┐    │ C │────►│ D │
              │    └───┘     └───┘
              │
              └───┐    ┌───┐
                  │ E │────► ...
                  └───┘
```

主要优势：

- **并行多个块** — 无需等待单一领导者。
- **无空时隙** — 如果验证者错过其轮次，DAG 会吸收它。
- **更快的确认** — 交易在数秒内确认，而非数分钟。

### 提示选择的工作原理

当创建一个新块时，它需要选择两个现有块进行连接。网络对最近块执行随机化搜索，自然倾向于最健康的分支，同时允许替代路径增长。这确保新块即使在高压下也能持续连接到 DAG 中连接最好的部分。

### 交易验证

每笔交易在进入网络前都要经过检查：

1. **格式检查** — 无重复输入，无负值。
2. **签名验证** — 发送者的签名被立即验证。
3. **双重支付预防** — 同一笔资金不能在待处理队列中被花费两次。
4. **费用检查** — 每笔交易必须包含最低费用。

无效交易在提交时被拒绝，而不是在块产生时。

---

## 权益证明与最终性

### 验证者的选择方式

每 12 秒，随机选择一个验证者来产生信标块。选择使用无偏的随机源，任何参与者都无法预测或操纵。

验证者通过存入至少 3200 万 BIG 进行注册。如果他们行为不当（例如签名冲突块），可能会受到惩罚。惩罚包括损失质押资金。

### 最终性的工作原理

在每个纪元结束时（每 32 个时隙，约 6.4 分钟），网络评估哪些块已获得足够的验证者支持：

- 当三分之二的质押验证者证明一个块时，该块变为**已证明**。
- 当其所扩展的链已经被最终化时，该块变为**已最终化**。
- 一旦最终化，一个块永远不能被撤销（除非所有质押验证者中的三分之一串通这样做）。

### 费用池与验证者奖励

系统具有**零通胀**——没有新代币作为块补贴被铸造。BIG 的总供应量在创世时固定，永远不会增加。

每笔交易支付费用。费用在整个纪元中累积，并在纪元边界时分配给验证者。每个验证者获得与其质押量成比例的奖励。这使激励相一致：验证者通过向网络投入更多资源获得更多收益，而整个奖励池完全来自网络使用而非货币扩张。

---

## 多层架构

### 一个 Layer 0，多个 Layer 1 链

全球范围内有**且仅有一个 Layer 0 链**。它是结算链，原生代币（BIG）在此创建，自定义代币在此发行。Layer 0 是代币供应的真相来源和所有价值转移的根源。

可以有**多个 Layer 1 链**，每个链独立运行，拥有自己的验证者、数据库和共识：

```
                    Layer 0（结算）
                           │
            ┌───────────────┼───────────────┐
            │               │               │
       L1-order        L1-contract       L1-payment
     （订单匹配）    （智能合约）    （仅转账）
            │               │               │
       L1-PAI           L1-NFT           （更多 L1...）
     （AI 提供商）   （非同质化代币）
```

每个 L1 链：
- 拥有自己的验证者和共识。
- 仅通过 L0 的桥接接收 BIG 代币（无原生铸造）。
- 完全隔离——一个 L1 的故障不会影响其他链。
- 使用自己的 `chainId` 防止一个链的块被另一个链接受。

### 水平可扩展性

由于每个 L1 链完全隔离且链 ID 可配置，您可以运行同一类型的多个实例：

```
CHAIN_ID=PAYMENT-US  →  美国区域的支付链
CHAIN_ID=PAYMENT-EU  →  欧洲区域的支付链
```

每个实例实现与 L0 相当的吞吐量。系统总吞吐量随 L1 实例数量线性扩展，无跨链协调开销。

### 可用的链类型

| 链 | 用途 |
|-------|---------|
| L0 结算 | BIG 铸造、代币创建、全局锚点 |
| L1 订单匹配 | 去中心化订单簿匹配 |
| L1 智能合约 | 通用合约执行 |
| L1 AI 提供商 | AI 提供商质押和信誉 |
| L1 NFT | 非同质化代币创建和转账 |
| L1 支付 | 仅转账（最小攻击面） |

---

## 后量子安全

Bigtangle 在每笔交易输入上使用两种后量子签名算法。两者均为 NIST 批准的最终标准：

| 算法 | 标准 | 安全等级 |
|-----------|----------|---------------|
| ML-DSA-87（Dilithium） | FIPS 204 | 第 5 级 |
| SLH-DSA-SHA2-256s（SPHINCS+） | FIPS 205 | 第 5 级 |

这两种算法依赖于数学上独立的假设（格密码学和哈希函数）。攻破其中一种并不会攻破另一种。攻击者需要同时攻破两者才能伪造单笔交易。

标准的 BIP39 种子短语确定性地生成两个密钥对。如果某种算法被攻破，系统支持升级到替代算法，而无需更改交易格式或使现有资金失效。

---

## 对比

| 指标 | Bigtangle | Solana | 以太坊 L1 | Visa |
|--------|-----------|--------|-------------|------|
| 账本 | DAG + 信标链 | 单链 | 单链 | 中心化 |
| 共识 | MCMC + Casper FFG | PoH + Tower BFT | Gasper | 权威机构 |
| 时隙时间 | 12s | 400ms | 12s | — |
| 最终性 | 约 12.8 分钟* | 约 12.8s | 约 6.4 分钟 | 即时 |
| 峰值 tx/s | 约 4,873 | 约 50,000* | 约 30 | 24,000 |
| 实际 tx/s | 约 4,873 | 约 2,000–3,000 | 约 15–30 | 约 1,700 |
| 并行执行 | DAG 原生 | Sealevel（分析） | 顺序 EVM | 分片数据库 |

\*供应商报告的实验室峰值；实际主网吞吐量显著较低。

\*\*Casper FFG 最终性（2 个纪元）。信标块确认约 24 秒（2 个时隙）。

### 关键优势

- **无单一领导者瓶颈** — 任何节点都可以创建交易块。信标块提议者提供最终性而不限制吞吐量。
- **水平扩展** — 额外的 L1 链增加容量而无需更改协议。
- **UTXO 模型** — 交易天然可并行，因为它们引用不同的输入。
- **后量子安全** — 双重签名从启动之时即生效。

---

## 结论

Bigtangle 将用于高吞吐量的并行 DAG 与用于确定性最终性的权益证明信标链相结合。这种架构消除了传统区块链中的单一领导者瓶颈，同时保留了强大的安全保证。

Layer 0 提供全局结算。独立的 Layer 1 链实现了跨专业化应用的水平扩展，而无需跨链协调开销。

总之，这些设计选择在一个统一的架构中提供了高交易吞吐量、快速确认、模块化可扩展性、固定代币供应和后量子安全性。

---

## 附录：开发者指南

### 节点配置

| 模块 | 端口 | 默认数据库 | 角色 |
|--------|------|------------|------|
| `layer0-server` | 8081 | `info_l0` | L0 全节点 |
| `layer0-mcmc` | 8082 | — | L0 共识 |
| `l1-order-server` | 8083 | `info_order` | L1 订单匹配 |
| `l1-payment-server` | 8091 | `info_payment` | L1 支付 |
| `l1-pai-server` | 8087 | `info_pai` | L1 AI 提供商 |
| `l1-nft-server` | 8089 | `info_nft` | L1 NFT |

### 环境变量

| 变量 | 默认值 | 描述 |
|----------|---------|-------------|
| `CHAIN_ID` | （可变） | L1 链标识符 |
| `FEE_DEFAULT` | 1000 | 最低交易费用（BIG） |
| `POS_SLOT_INTERVAL_MS` | 12000 | 时隙持续时间（毫秒） |
| `POS_SLOTS_PER_EPOCH` | 32 | 每个纪元的时隙数 |

### 各链的块类型范围

| 链 | 接受的块类型 |
|-------|---------------------|
| L0 | 所有类型 |
| L1 订单匹配 | INITIAL, TRANSFER, BEACON, ORDER_OPEN, ORDER_CANCEL |
| L1 智能合约 | INITIAL, TRANSFER, BEACON, CONTRACT_EVENT, CONTRACTEVENT_CANCEL |
| L1 PAI | INITIAL, TRANSFER, BEACON, CONTRACT_EVENT, CONTRACTEVENT_CANCEL |
| L1 NFT | 所有类型（重用 L0 参数） |
| L1 支付 | 仅 INITIAL, TRANSFER, BEACON |

### 奖励计算

在每个纪元边界，累积的费用被分配：

```
对于每个活跃验证者：
  reward = validator.stake × pool / total_active_stake
```

### 每个块的费用累积

```
对于块中的每笔交易：
  surplus = sum_of_BIG_inputs − sum_of_BIG_outputs
  if surplus > 0:
    add surplus to accumulated_fee_pool
```

### 后量子密钥派生

24 词 BIP39 种子短语（256 位熵）通过 HKDF-SHA256 确定性地生成两个后量子密钥对。前 32 字节种子用于 ML-DSA-87，后 32 字节种子用于 SLH-DSA-SHA2-256s。

### 块大小预算（后量子签名）

| 组件 | 大小 |
|-----------|------|
| ML-DSA-87 签名 | 4.6 KB |
| SLH-DSA-256s 签名 | 16 KB |
| 每个输入总计 | 约 23 KB |
| 100 tx/块 | 约 3 MB（可行） |
| 500 tx/块 | 约 12 MB（可行） |
| 2,000 tx/块 | 约 48 MB（需要 100 MB 限制） |
